@model IEnumerable<ManufacturerModel>
<h3>Справочник производителей</h3>
<div class="div-box">
    <a asp-area="Admin" asp-controller="Manufacturer" asp-action="Edit" asp-route-id="">Добавить производителя</a>
</div>

<div>
    <div>
        @if (Model.Any())
        {
            <div>
                @foreach (ManufacturerModel item in Model)
                {
                    <div>
                        <a asp-area="Admin" asp-controller="Manufacturer" asp-action="Edit" asp-route-id="@item.Id">редактировать</a>
                        |
                        <form style="display: inline-block;" id="form-@item.Id" asp-area="Admin" asp-controller="Manufacturer" asp-action="Delete" asp-route-id="@item.Id" method="post">
                            <input type="hidden" name="id" value="@item.Id">
                            <a href="#" onclick="document.getElementById('form-@item.Id').submit();">удалить</a>
                        </form>
                        |
                        <a asp-area="" asp-controller="Manufacturer" asp-action="Index" asp-route-id="@item.Id">
                            @($"{item.ManufacturerName}")
                        </a>
                    </div>
                }
            </div>
        }
    </div>
</div>


